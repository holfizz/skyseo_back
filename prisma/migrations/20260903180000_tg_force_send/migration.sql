-- Разрешение рассылать с непрогретого аккаунта — по решению владельца.
-- Только добавление колонки со значением по умолчанию: поведение существующих
-- аккаунтов не меняется, пока флаг не поставят руками.
ALTER TABLE "tg_accounts" ADD COLUMN IF NOT EXISTS "forceSend" BOOLEAN NOT NULL DEFAULT false;
