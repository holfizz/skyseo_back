-- Аватар аккаунта и цель дня у кампании.

-- Аватар как data-URI: снимается при проверке и хранится у нас, чтобы список
-- не тянул по отдельной загрузке файла на каждый аккаунт.
ALTER TABLE "tg_accounts" ADD COLUMN "avatar" TEXT;

-- Цель дня по кампании. Норма каждого аккаунта считается из неё пропорционально
-- готовности; perAccountPerDay остаётся жёстким потолком сверху.
ALTER TABLE "tg_campaigns" ADD COLUMN "dailyGoal" INTEGER;
