-- Признак «телеграм вписан руками в админке».
-- По умолчанию false у всех существующих лидов: провенанс старых хэндлов
-- неизвестен, а показывать менеджеру спарсенный контакт нельзя. Владелец
-- пометит нужные, просто пересохранив телеграм в карточке лида.
ALTER TABLE "outreach_leads"
  ADD COLUMN IF NOT EXISTS "telegramManual" BOOLEAN NOT NULL DEFAULT false;
