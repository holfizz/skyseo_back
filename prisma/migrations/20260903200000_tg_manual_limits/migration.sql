-- Ручное деление дневного лимита между аккаунтами кампании.
-- Только добавление колонки, значение по умолчанию NULL: у всех существующих
-- связок поведение прежнее — норма считается автоматически по готовности.
ALTER TABLE "tg_campaign_accounts" ADD COLUMN IF NOT EXISTS "dailyLimit" INTEGER;
